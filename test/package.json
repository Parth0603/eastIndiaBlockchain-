{
  "name": "disaster-relief-tests",
  "version": "1.0.0",
  "description": "End-to-end and integration tests for disaster relief system",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:e2e": "vitest --run integration/",
    "test:properties": "vitest --run properties/"
  },
  "devDependencies": {
    "vitest": "^1.6.1",
    "supertest": "^6.3.3",
    "fast-check": "^4.5.3",
    "ethers": "^6.8.0"
  }
}